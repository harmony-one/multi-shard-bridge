import React from 'react';
import { Icon } from '../../../components/Base';

interface Props {}

export const NetworkIcon: React.FC<Props> = ({}) => {
  return <Icon size="32" glyph="Harmony" />;
};

NetworkIcon.displayName = 'NetworkIcon';
